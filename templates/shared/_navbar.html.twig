<nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}"><h4>Symphonic Team</h4></a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0" id="menu">
            {% if app.user %}
              <li class="nav-item">
                <a class="nav-link" href="{{path('event_list')}}">Parcourir</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{path('event_create')}}"
                  >Proposer une séance</a
                >
              </li>
            {% endif %}
          </ul>
          <ul class="p-0 navbar-nav">
            {% if app.user %}
            
              <li class="nav-item dropdown d-flex">
              <img 
                class="button avatar"
                {% if app.user.avatar is not same as null %}
                  src="{{app.user.avatar}}"
                {% else %}
                  src="{{asset('images/default-avatar.jpg')}}"
                {% endif %}
                alt="photo"
              />
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Bonjour {{app.user.firstName}}
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <li><a class="dropdown-item" href="#">Mon compte</a></li>
                  <li><a class="dropdown-item" href="{{path('user_event_list')}}">Séances que je propose</a></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><a class="dropdown-item" href="{{path('user_participate_list')}}">Séances auxquelles je participe</a></li>
                  <li><a class="dropdown-item" href="{{path('user_interested_list')}}">Séances qui m'intéressent</a></li>
                </ul>
              </li>

              {% if app.user.roles[0] is same as "ROLE_ADMIN" %}
                <li class="nav-item">
                  <a href="{{ path('admin_home') }}" class="nav-link"
                  >Espace admin
                  </a>
                </li>
              {% endif %}
              <li>
                <a href="{{ path('app_logout') }}" class="btn btn-outline-primary"
                >Se déconnecter
                </a>
              </li>

              {% else %}

                <li>
                  <a href="{{ path('app_login') }}" class="btn btn-outline-primary me-1"
                  >S'identifier
                  </a>
                </li>

                <li>
                  <a href="{{ path('app_register') }}" class="btn btn-primary"
                  >S'inscrire
                  </a>
                </li>

            {% endif %}
          </ul>
        </div>
      </div>
    </nav>