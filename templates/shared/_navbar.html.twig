<nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}"><h4>Symphonic Team</h4></a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0" id="menu">
            <li class="nav-item">
              <a class="nav-link" href="#!">Parcourir</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#!"
                >Proposer une séance</a
              >
            </li>
          </ul>
          <ul class="p-0 navbar-nav">
            {% if app.user %}
            
              <li class="nav-item align-self-center me-3">
                <p class="user-name">Bonjour {{app.user.pseudo}}</p>
              </li>

              <li>
                <a href="{{ path('app_logout') }}" class="btn btn-outline-primary"
                >Se déconnecter
                </a>
              </li>

              {% else %}

                <li>
                  <a href="{{ path('app_login') }}" class="btn btn-outline-primary me-1"
                  >S'identifier
                  </a>
                </li>

                <li>
                  <a href="{{ path('app_register') }}" class="btn btn-primary"
                  >S'inscrire
                  </a>
                </li>

            {% endif %}
          </ul>
        </div>
      </div>
    </nav>